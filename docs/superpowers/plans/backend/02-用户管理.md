# 02 - 用户管理

> 模块: 用户 CRUD API + Pydantic Schemas
> 优先级: P0
> 依赖: 01-数据库模型
> 预计时间: 0.5天

## 一、目标

实现用户管理的完整 RESTful API，包括注册、查询、更新、删除功能。

## 二、API 接口设计

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/users | 获取用户列表（分页） |
| GET | /api/users/{id} | 获取单个用户 |
| POST | /api/users | 注册新用户（含人脸图片） |
| PUT | /api/users/{id} | 更新用户信息 |
| DELETE | /api/users/{id} | 删除用户 |

## 三、代码实现

### 3.1 创建目录

```bash
mkdir -p backend/app/schemas backend/app/api
touch backend/app/schemas/__init__.py
touch backend/app/api/__init__.py
```

### 3.2 Pydantic Schemas (app/schemas/user.py)

```python
from pydantic import BaseModel, Field
from typing import Optional
from datetime import datetime

# 基础模型
class UserBase(BaseModel):
    employee_id: str = Field(..., min_length=1, max_length=50, description="工号")
    name: str = Field(..., min_length=1, max_length=100, description="姓名")
    department: Optional[str] = Field(None, max_length=100, description="部门")

# 创建请求
class UserCreate(UserBase):
    pass

# 更新请求
class UserUpdate(BaseModel):
    name: Optional[str] = Field(None, max_length=100)
    department: Optional[str] = Field(None, max_length=100)
    status: Optional[int] = Field(None, ge=0, le=1)

# 响应模型
class UserResponse(UserBase):
    id: int
    status: int
    face_encoding_path: Optional[str] = None
    face_image_path: Optional[str] = None
    created_at: Optional[datetime] = None
    updated_at: Optional[datetime] = None
    
    class Config:
        from_attributes = True  # Pydantic v2

# 列表响应
class UserListResponse(BaseModel):
    items: list[UserResponse]
    total: int
    page: int
    page_size: int

# 注册响应（扩展）
class UserRegisterResponse(UserResponse):
    face_detected: bool = False
    face_quality: Optional[str] = None  # good/medium/poor
```

### 3.3 用户 API (app/api/users.py)

```python
from fastapi import APIRouter, Depends, HTTPException, Query, UploadFile, File, Form
from sqlalchemy.orm import Session
from typing import Optional
from app.database import get_db
from app.models import User
from app.schemas.user import (
    UserCreate, UserUpdate, UserResponse, UserListResponse, UserRegisterResponse
)

router = APIRouter(prefix="/api/users", tags=["用户管理"])

@router.get("", response_model=UserListResponse, summary="获取用户列表")
def list_users(
    page: int = Query(1, ge=1, description="页码"),
    page_size: int = Query(10, ge=1, le=100, description="每页数量"),
    department: Optional[str] = Query(None, description="部门筛选"),
    status: Optional[int] = Query(None, ge=0, le=1, description="状态筛选"),
    db: Session = Depends(get_db)
):
    """获取用户列表（支持分页和筛选）"""
    query = db.query(User)
    
    # 筛选
    if department:
        query = query.filter(User.department == department)
    if status is not None:
        query = query.filter(User.status == status)
    
    # 总数
    total = query.count()
    
    # 分页
    offset = (page - 1) * page_size
    users = query.offset(offset).limit(page_size).all()
    
    return UserListResponse(
        items=[UserResponse.model_validate(u) for u in users],
        total=total,
        page=page,
        page_size=page_size
    )

@router.get("/{user_id}", response_model=UserResponse, summary="获取用户详情")
def get_user(user_id: int, db: Session = Depends(get_db)):
    """获取单个用户信息"""
    user = db.query(User).filter(User.id == user_id).first()
    if not user:
        raise HTTPException(status_code=404, detail="用户不存在")
    return UserResponse.model_validate(user)

@router.post("", response_model=UserRegisterResponse, summary="注册新用户")
def register_user(
    employee_id: str = Form(..., description="工号"),
    name: str = Form(..., description="姓名"),
    department: Optional[str] = Form(None, description="部门"),
    face_image: Optional[UploadFile] = File(None, description="人脸照片"),
    db: Session = Depends(get_db)
):
    """
    注册新用户
    
    - 如果提供 face_image，将进行人脸检测和质量评估
    - 人脸特征提取将在 03-人脸识别核心 模块实现
    """
    # 检查工号是否已存在
    exists = db.query(User).filter(User.employee_id == employee_id).first()
    if exists:
        raise HTTPException(status_code=400, detail="工号已存在")
    
    # 创建用户
    user = User(
        employee_id=employee_id,
        name=name,
        department=department,
        status=1
    )
    db.add(user)
    db.commit()
    db.refresh(user)
    
    # TODO: 人脸处理将在 03-人脸识别核心 模块实现
    face_detected = False
    face_quality = None
    
    if face_image:
        # 临时标记：后续实现人脸检测
        face_detected = True
        face_quality = "pending"
    
    response = UserRegisterResponse.model_validate(user)
    response.face_detected = face_detected
    response.face_quality = face_quality
    
    return response

@router.put("/{user_id}", response_model=UserResponse, summary="更新用户信息")
def update_user(
    user_id: int,
    user_update: UserUpdate,
    db: Session = Depends(get_db)
):
    """更新用户基本信息"""
    user = db.query(User).filter(User.id == user_id).first()
    if not user:
        raise HTTPException(status_code=404, detail="用户不存在")
    
    # 更新字段
    update_data = user_update.model_dump(exclude_unset=True)
    for field, value in update_data.items():
        setattr(user, field, value)
    
    db.commit()
    db.refresh(user)
    
    return UserResponse.model_validate(user)

@router.delete("/{user_id}", summary="删除用户")
def delete_user(user_id: int, db: Session = Depends(get_db)):
    """删除用户（同时删除人脸数据）"""
    user = db.query(User).filter(User.id == user_id).first()
    if not user:
        raise HTTPException(status_code=404, detail="用户不存在")
    
    # TODO: 删除人脸图片和特征文件（03模块实现）
    
    db.delete(user)
    db.commit()
    
    return {"code": 200, "message": "删除成功"}
```

### 3.4 注册路由 (app/api/__init__.py)

```python
from fastapi import APIRouter
from app.api.users import router as users_router

api_router = APIRouter()
api_router.include_router(users_router)
```

### 3.5 更新 main.py

```python
# app/main.py 添加路由
from app.api import api_router

app.include_router(api_router)
```

## 四、实现步骤

```bash
# Step 1: 创建文件
touch app/schemas/user.py
touch app/api/users.py

# Step 2: 编写代码（如上）

# Step 3: 重启服务
python run.py

# Step 4: 测试 API
# 浏览器访问 http://localhost:8000/docs
```

## 五、API 测试用例

### 5.1 注册用户

```bash
curl -X POST "http://localhost:8000/api/users" \
  -F "employee_id=10001" \
  -F "name=张三" \
  -F "department=技术部"
```

预期响应:
```json
{
  "id": 1,
  "employee_id": "10001",
  "name": "张三",
  "department": "技术部",
  "status": 1,
  "face_detected": false,
  "face_quality": null,
  ...
}
```

### 5.2 获取用户列表

```bash
curl "http://localhost:8000/api/users"
```

### 5.3 获取单个用户

```bash
curl "http://localhost:8000/api/users/1"
```

### 5.4 更新用户

```bash
curl -X PUT "http://localhost:8000/api/users/1" \
  -H "Content-Type: application/json" \
  -d '{"department": "产品部"}'
```

### 5.5 删除用户

```bash
curl -X DELETE "http://localhost:8000/api/users/1"
```

## 六、验收标准

- [ ] 所有 API 端点在 Swagger 文档中可见
- [ ] POST /api/users 可成功创建用户
- [ ] GET /api/users 返回分页列表
- [ ] GET /api/users/{id} 返回单个用户
- [ ] PUT /api/users/{id} 可更新用户信息
- [ ] DELETE /api/users/{id} 可删除用户
- [ ] 工号重复时返回 400 错误
- [ ] 用户不存在时返回 404 错误
- [ ] 分页和筛选功能正常

## 七、注意事项

1. **人脸图片处理**: 本模块仅预留接口，实际处理在 03-人脸识别核心 模块实现
2. **文件上传**: 使用 `multipart/form-data` 格式
3. **事务管理**: 确保数据库操作的原子性
4. **日志记录**: 后续添加操作日志（08-测试与优化 模块）
