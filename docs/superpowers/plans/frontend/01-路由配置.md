# 01 - 路由配置

> 模块: Vue Router 路由管理
> 优先级: P0
> 依赖: 00-项目初始化
> 预计时间: 0.5天

## 一、目标

配置 Vue Router，实现页面路由导航，支持路由懒加载。

## 二、路由规划

| 路径 | 页面 | 名称 | 说明 |
|------|------|------|------|
| `/` | Home.vue | home | 首页导航 |
| `/register` | Register.vue | register | 用户注册 |
| `/scan` | Scan.vue | scan | 实时刷脸 |
| `/records` | Records.vue | records | 考勤记录 |
| `/users` | Users.vue | users | 用户管理（可选） |
| `*` | NotFound.vue | not-found | 404页面 |

## 三、代码实现

### 3.1 路由配置 (src/router/index.ts)

```typescript
import { createRouter, createWebHistory, RouteRecordRaw } from 'vue-router'

const routes: RouteRecordRaw[] = [
  {
    path: '/',
    name: 'home',
    component: () => import('@/views/Home.vue'),
    meta: {
      title: '首页'
    }
  },
  {
    path: '/register',
    name: 'register',
    component: () => import('@/views/Register.vue'),
    meta: {
      title: '用户注册'
    }
  },
  {
    path: '/scan',
    name: 'scan',
    component: () => import('@/views/Scan.vue'),
    meta: {
      title: '实时刷脸'
    }
  },
  {
    path: '/records',
    name: 'records',
    component: () => import('@/views/Records.vue'),
    meta: {
      title: '考勤记录'
    }
  },
  {
    path: '/users',
    name: 'users',
    component: () => import('@/views/Users.vue'),
    meta: {
      title: '用户管理'
    }
  },
  {
    path: '/:pathMatch(.*)*',
    name: 'not-found',
    component: () => import('@/views/NotFound.vue'),
    meta: {
      title: '页面未找到'
    }
  }
]

const router = createRouter({
  history: createWebHistory(),
  routes
})

// 路由守卫 - 设置页面标题
router.beforeEach((to, from, next) => {
  const title = to.meta.title as string
  if (title) {
    document.title = `${title} - 人脸识别门禁系统`
  }
  next()
})

export default router
```

### 3.2 导航组件 (src/components/NavBar.vue)

```vue
<template>
  <el-menu
    :default-active="activeIndex"
    mode="horizontal"
    @select="handleSelect"
    background-color="#545c64"
    text-color="#fff"
    active-text-color="#ffd04b"
  >
    <el-menu-item index="/">
      <el-icon><HomeFilled /></el-icon>
      <span>首页</span>
    </el-menu-item>
    
    <el-menu-item index="/register">
      <el-icon><UserFilled /></el-icon>
      <span>用户注册</span>
    </el-menu-item>
    
    <el-menu-item index="/scan">
      <el-icon><Camera /></el-icon>
      <span>实时刷脸</span>
    </el-menu-item>
    
    <el-menu-item index="/records">
      <el-icon><Document /></el-icon>
      <span>考勤记录</span>
    </el-menu-item>
    
    <el-menu-item index="/users">
      <el-icon><User /></el-icon>
      <span>用户管理</span>
    </el-menu-item>
  </el-menu>
</template>

<script setup lang="ts">
import { computed } from 'vue'
import { useRouter, useRoute } from 'vue-router'
import {
  HomeFilled,
  UserFilled,
  Camera,
  Document,
  User
} from '@element-plus/icons-vue'

const router = useRouter()
const route = useRoute()

const activeIndex = computed(() => route.path)

const handleSelect = (index: string) => {
  router.push(index)
}
</script>

<style scoped>
.el-menu {
  border-bottom: none;
}
</style>
```

### 3.3 布局组件 (src/layouts/MainLayout.vue)

```vue
<template>
  <div class="main-layout">
    <!-- 顶部导航 -->
    <header class="header">
      <div class="logo">
        <el-icon :size="32"><Monitor /></el-icon>
        <span>人脸识别门禁系统</span>
      </div>
      <NavBar />
    </header>
    
    <!-- 主内容区 -->
    <main class="main">
      <router-view v-slot="{ Component }">
        <transition name="fade" mode="out-in">
          <component :is="Component" />
        </transition>
      </router-view>
    </main>
    
    <!-- 底部 -->
    <footer class="footer">
      <p>© 2026 Face Recognition Access Control System</p>
    </footer>
  </div>
</template>

<script setup lang="ts">
import { Monitor } from '@element-plus/icons-vue'
import NavBar from '@/components/NavBar.vue'
</script>

<style scoped>
.main-layout {
  display: flex;
  flex-direction: column;
  min-height: 100vh;
}

.header {
  display: flex;
  align-items: center;
  background-color: #545c64;
  padding: 0 20px;
}

.logo {
  display: flex;
  align-items: center;
  gap: 10px;
  color: #fff;
  font-size: 18px;
  font-weight: bold;
  margin-right: 40px;
}

.main {
  flex: 1;
  padding: 20px;
  background-color: #f0f2f5;
  overflow: auto;
}

.footer {
  padding: 15px;
  text-align: center;
  background-color: #fff;
  color: #666;
  font-size: 14px;
}

/* 路由过渡动画 */
.fade-enter-active,
.fade-leave-active {
  transition: opacity 0.3s ease;
}

.fade-enter-from,
.fade-leave-to {
  opacity: 0;
}
</style>
```

### 3.4 首页 (src/views/Home.vue)

```vue
<template>
  <div class="home">
    <el-row :gutter="20">
      <el-col :span="6" v-for="item in menuItems" :key="item.path">
        <el-card
          class="menu-card"
          shadow="hover"
          @click="$router.push(item.path)"
        >
          <div class="card-content">
            <el-icon :size="60" :color="item.color">
              <component :is="item.icon" />
            </el-icon>
            <h3>{{ item.title }}</h3>
            <p>{{ item.description }}</p>
          </div>
        </el-card>
      </el-col>
    </el-row>
    
    <!-- 统计卡片 -->
    <el-row :gutter="20" style="margin-top: 30px">
      <el-col :span="8">
        <el-card class="stat-card">
          <el-statistic title="注册用户" :value="stats.userCount">
            <template #prefix>
              <el-icon><User /></el-icon>
            </template>
          </el-statistic>
        </el-card>
      </el-col>
      <el-col :span="8">
        <el-card class="stat-card">
          <el-statistic title="今日打卡" :value="stats.todayCount">
            <template #prefix>
              <el-icon><Clock /></el-icon>
            </template>
          </el-statistic>
        </el-card>
      </el-col>
      <el-col :span="8">
        <el-card class="stat-card">
          <el-statistic title="识别成功率" :value="stats.successRate" suffix="%">
            <template #prefix>
              <el-icon><SuccessFilled /></el-icon>
            </template>
          </el-statistic>
        </el-card>
      </el-col>
    </el-row>
  </div>
</template>

<script setup lang="ts">
import { ref, onMounted } from 'vue'
import {
  UserFilled,
  Camera,
  Document,
  User,
  Clock,
  SuccessFilled
} from '@element-plus/icons-vue'

const menuItems = [
  {
    path: '/register',
    title: '用户注册',
    description: '录入新用户和人脸信息',
    icon: UserFilled,
    color: '#409EFF'
  },
  {
    path: '/scan',
    title: '实时刷脸',
    description: '门禁识别和打卡',
    icon: Camera,
    color: '#67C23A'
  },
  {
    path: '/records',
    title: '考勤记录',
    description: '查询和导出考勤',
    icon: Document,
    color: '#E6A23C'
  },
  {
    path: '/users',
    title: '用户管理',
    description: '管理已注册用户',
    icon: User,
    color: '#F56C6C'
  }
]

const stats = ref({
  userCount: 0,
  todayCount: 0,
  successRate: 0
})

onMounted(async () => {
  // TODO: 调用 API 获取统计数据
  stats.value = {
    userCount: 12,
    todayCount: 24,
    successRate: 95.5
  }
})
</script>

<style scoped>
.home {
  max-width: 1200px;
  margin: 0 auto;
}

.menu-card {
  cursor: pointer;
  transition: all 0.3s;
}

.menu-card:hover {
  transform: translateY(-5px);
}

.card-content {
  text-align: center;
  padding: 20px;
}

.card-content h3 {
  margin: 15px 0 10px;
  font-size: 18px;
}

.card-content p {
  color: #999;
  font-size: 14px;
}

.stat-card {
  text-align: center;
}
</style>
```

### 3.5 404页面 (src/views/NotFound.vue)

```vue
<template>
  <div class="not-found">
    <el-result
      icon="warning"
      title="404"
      sub-title="抱歉，您访问的页面不存在"
    >
      <template #extra>
        <el-button type="primary" @click="$router.push('/')">
          返回首页
        </el-button>
      </template>
    </el-result>
  </div>
</template>

<style scoped>
.not-found {
  display: flex;
  justify-content: center;
  align-items: center;
  height: 100%;
}
</style>
```

### 3.6 更新 App.vue 使用布局

```vue
<template>
  <div id="app">
    <el-config-provider :locale="zhCn">
      <MainLayout />
    </el-config-provider>
  </div>
</template>

<script setup lang="ts">
import { ElConfigProvider } from 'element-plus'
import zhCn from 'element-plus/dist/locale/zh-cn.mjs'
import MainLayout from '@/layouts/MainLayout.vue'
</script>

<style>
#app {
  width: 100%;
  height: 100vh;
}
</style>
```

## 四、创建文件

```bash
# 创建目录
mkdir -p src/layouts src/views

# 创建文件
touch src/router/index.ts
touch src/layouts/MainLayout.vue
touch src/components/NavBar.vue
touch src/views/{Home,NotFound}.vue
```

## 五、验收标准

- [ ] 所有路由可正常访问
- [ ] 导航栏高亮显示当前页面
- [ ] 页面切换有过渡动画
- [ ] 浏览器后退/前进按钮正常工作
- [ ] 页面标题随路由变化
- [ ] 404页面正常显示

## 六、路由元信息

可在 `meta` 中添加更多信息：

```typescript
{
  path: '/scan',
  meta: {
    title: '实时刷脸',
    requiresAuth: false,  // 是否需要登录
    keepAlive: true       // 是否缓存组件
  }
}
```

## 七、下一步

完成路由配置后，继续实现：
- **02-状态管理**: Pinia 状态管理
- **03-API封装**: Axios 请求封装
