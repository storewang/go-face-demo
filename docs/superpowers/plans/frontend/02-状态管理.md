# 02 - 状态管理

> 模块: Pinia 状态管理
> 优先级: P0
> 依赖: 01-路由配置
> 预计时间: 0.5天

## 一、目标

使用 Pinia 管理全局状态，包括用户信息、考勤数据等。

## 二、状态设计

| Store | 用途 | 主要状态 |
|-------|------|---------|
| user | 用户管理 | 用户列表、当前用户 |
| attendance | 考勤管理 | 考勤记录、统计数据 |
| app | 应用状态 | 加载状态、通知消息 |

## 三、代码实现

### 3.1 用户 Store (src/stores/user.ts)

```typescript
import { defineStore } from 'pinia'
import { ref, computed } from 'vue'
import type { User } from '@/types/user'
import * as userApi from '@/api/user'

export const useUserStore = defineStore('user', () => {
  // 状态
  const users = ref<User[]>([])
  const currentUser = ref<User | null>(null)
  const loading = ref(false)
  
  // 计算属性
  const userCount = computed(() => users.value.length)
  const activeUsers = computed(() => 
    users.value.filter(u => u.status === 1)
  )
  
  // 方法
  async function fetchUsers(params?: any) {
    loading.value = true
    try {
      const res = await userApi.getUsers(params)
      users.value = res.items
      return res
    } finally {
      loading.value = false
    }
  }
  
  async function createUser(data: any) {
    loading.value = true
    try {
      const user = await userApi.createUser(data)
      users.value.push(user)
      return user
    } finally {
      loading.value = false
    }
  }
  
  async function deleteUser(id: number) {
    loading.value = true
    try {
      await userApi.deleteUser(id)
      users.value = users.value.filter(u => u.id !== id)
    } finally {
      loading.value = false
    }
  }
  
  function setCurrentUser(user: User | null) {
    currentUser.value = user
  }
  
  return {
    // 状态
    users,
    currentUser,
    loading,
    // 计算属性
    userCount,
    activeUsers,
    // 方法
    fetchUsers,
    createUser,
    deleteUser,
    setCurrentUser
  }
})
```

### 3.2 考勤 Store (src/stores/attendance.ts)

```typescript
import { defineStore } from 'pinia'
import { ref, computed } from 'vue'
import type { AttendanceRecord, AttendanceStats } from '@/types/attendance'
import * as attendanceApi from '@/api/attendance'

export const useAttendanceStore = defineStore('attendance', () => {
  // 状态
  const records = ref<AttendanceRecord[]>([])
  const stats = ref<AttendanceStats>({
    total_records: 0,
    success_count: 0,
    failed_count: 0,
    unique_users: 0,
    avg_confidence: 0
  })
  const loading = ref(false)
  
  // 计算属性
  const todayRecords = computed(() => {
    const today = new Date().toDateString()
    return records.value.filter(r => 
      new Date(r.created_at).toDateString() === today
    )
  })
  
  // 方法
  async function fetchRecords(params?: any) {
    loading.value = true
    try {
      const res = await attendanceApi.getAttendance(params)
      records.value = res.items
      return res
    } finally {
      loading.value = false
    }
  }
  
  async function checkIn(image: File) {
    loading.value = true
    try {
      const result = await attendanceApi.checkIn(image)
      if (result.success) {
        // 添加到记录列表
        await fetchRecords()
      }
      return result
    } finally {
      loading.value = false
    }
  }
  
  async function checkOut(image: File) {
    loading.value = true
    try {
      const result = await attendanceApi.checkOut(image)
      if (result.success) {
        await fetchRecords()
      }
      return result
    } finally {
      loading.value = false
    }
  }
  
  async function fetchStats(params?: any) {
    try {
      const data = await attendanceApi.getStats(params)
      stats.value = data
    } catch (error) {
      console.error('Failed to fetch stats:', error)
    }
  }
  
  async function exportExcel(params: any) {
    try {
      const blob = await attendanceApi.exportAttendance(params)
      return blob
    } catch (error) {
      console.error('Failed to export:', error)
      throw error
    }
  }
  
  return {
    // 状态
    records,
    stats,
    loading,
    // 计算属性
    todayRecords,
    // 方法
    fetchRecords,
    checkIn,
    checkOut,
    fetchStats,
    exportExcel
  }
})
```

### 3.3 应用 Store (src/stores/app.ts)

```typescript
import { defineStore } from 'pinia'
import { ref } from 'vue'
import { ElMessage } from 'element-plus'

export const useAppStore = defineStore('app', () => {
  // 状态
  const loading = ref(false)
  const loadingText = ref('加载中...')
  const notifications = ref<any[]>([])
  
  // 方法
  function showLoading(text: string = '加载中...') {
    loading.value = true
    loadingText.value = text
  }
  
  function hideLoading() {
    loading.value = false
  }
  
  function showMessage(message: string, type: 'success' | 'error' | 'warning' | 'info' = 'info') {
    ElMessage({
      message,
      type,
      duration: 3000
    })
  }
  
  function showSuccess(message: string) {
    showMessage(message, 'success')
  }
  
  function showError(message: string) {
    showMessage(message, 'error')
  }
  
  function showWarning(message: string) {
    showMessage(message, 'warning')
  }
  
  function addNotification(notification: any) {
    notifications.value.push({
      id: Date.now(),
      ...notification,
      read: false,
      created_at: new Date()
    })
  }
  
  function markAsRead(id: number) {
    const notification = notifications.value.find(n => n.id === id)
    if (notification) {
      notification.read = true
    }
  }
  
  return {
    // 状态
    loading,
    loadingText,
    notifications,
    // 方法
    showLoading,
    hideLoading,
    showMessage,
    showSuccess,
    showError,
    showWarning,
    addNotification,
    markAsRead
  }
})
```

### 3.4 Store 导出 (src/stores/index.ts)

```typescript
export { useUserStore } from './user'
export { useAttendanceStore } from './attendance'
export { useAppStore } from './app'
```

## 四、类型定义

### 4.1 用户类型 (src/types/user.ts)

```typescript
export interface User {
  id: number
  employee_id: string
  name: string
  department?: string
  status: number
  face_encoding_path?: string
  face_image_path?: string
  created_at?: string
  updated_at?: string
}

export interface UserListResponse {
  items: User[]
  total: number
  page: number
  page_size: number
}

export interface UserCreateRequest {
  employee_id: string
  name: string
  department?: string
  face_image?: File
}
```

### 4.2 考勤类型 (src/types/attendance.ts)

```typescript
export type ActionType = 'CHECK_IN' | 'CHECK_OUT'
export type ResultType = 'SUCCESS' | 'FAILED'

export interface AttendanceRecord {
  id: number
  user_id?: number
  employee_id?: string
  name?: string
  action_type: ActionType
  confidence?: number
  snapshot_path?: string
  result: ResultType
  created_at: string
}

export interface AttendanceListResponse {
  items: AttendanceRecord[]
  total: number
  page: number
  page_size: number
}

export interface AttendanceStats {
  total_records: number
  success_count: number
  failed_count: number
  unique_users: number
  avg_confidence: number
}

export interface CheckInResponse {
  success: boolean
  action_type: string
  user?: {
    id: number
    employee_id: string
    name: string
  }
  confidence?: number
  message: string
  record_id?: number
}
```

## 五、使用示例

### 5.1 在组件中使用

```vue
<script setup lang="ts">
import { onMounted } from 'vue'
import { useUserStore, useAppStore } from '@/stores'

const userStore = useUserStore()
const appStore = useAppStore()

onMounted(async () => {
  try {
    appStore.showLoading('加载用户数据...')
    await userStore.fetchUsers()
    appStore.showSuccess('数据加载成功')
  } catch (error) {
    appStore.showError('数据加载失败')
  } finally {
    appStore.hideLoading()
  }
})
</script>
```

### 5.2 在 setup 外使用

```typescript
import { useUserStore } from '@/stores'

// 在函数中使用
async function someFunction() {
  const userStore = useUserStore()
  await userStore.fetchUsers()
}
```

## 六、创建文件

```bash
# 创建目录
mkdir -p src/stores src/types

# 创建文件
touch src/stores/{index,user,attendance,app}.ts
touch src/types/{user,attendance}.ts
```

## 七、验收标准

- [ ] 所有 Store 正常创建
- [ ] 状态可正常读写
- [ ] 计算属性正常工作
- [ ] 异步方法可正常调用
- [ ] TypeScript 类型正确
- [ ] 组件中可正常使用 Store

## 八、最佳实践

1. **使用组合式 API**: 使用 `setup()` 语法定义 Store
2. **解构使用**: 使用 `storeToRefs()` 解构响应式状态
3. **类型安全**: 为所有状态和方法添加类型
4. **错误处理**: 在 Store 中统一处理错误
5. **命名规范**: Store 文件名和方法名清晰明确

## 九、下一步

完成状态管理后，继续实现：
- **03-API封装**: Axios 请求封装
- **04-摄像头组件**: 摄像头访问组件
