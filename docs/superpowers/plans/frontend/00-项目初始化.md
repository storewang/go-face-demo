# 00 - 项目初始化

> 模块: Vue 3 + Vite + TypeScript 项目搭建
> 优先级: P0
> 预计时间: 0.5天

## 一、目标

使用 Vite 创建 Vue 3 + TypeScript 项目，安装必要依赖，配置开发环境。

## 二、技术栈

| 技术 | 版本 | 用途 |
|------|------|------|
| Vue | ^3.4.0 | 前端框架 |
| Vite | ^5.0.0 | 构建工具 |
| TypeScript | ^5.0.0 | 类型支持 |
| Vue Router | ^4.2.0 | 路由管理 |
| Pinia | ^2.1.0 | 状态管理 |
| Element Plus | ^2.4.0 | UI组件库 |
| Axios | ^1.6.0 | HTTP客户端 |

## 三、项目结构

```
frontend/
├── public/
│   └── favicon.ico
├── src/
│   ├── api/                 # API 接口
│   │   ├── index.ts
│   │   ├── user.ts
│   │   ├── face.ts
│   │   └── attendance.ts
│   ├── assets/              # 静态资源
│   │   └── styles/
│   │       └── main.css
│   ├── components/          # 公共组件
│   │   ├── Camera.vue
│   │   ├── FaceCapture.vue
│   │   └── NavBar.vue
│   ├── composables/         # 组合式函数
│   │   ├── useCamera.ts
│   │   └── useWebSocket.ts
│   ├── router/              # 路由配置
│   │   └── index.ts
│   ├── stores/              # Pinia 状态
│   │   ├── user.ts
│   │   └── attendance.ts
│   ├── types/               # TypeScript 类型
│   │   ├── user.ts
│   │   ├── face.ts
│   │   └── attendance.ts
│   ├── utils/               # 工具函数
│   │   ├── request.ts
│   │   └── image.ts
│   ├── views/               # 页面组件
│   │   ├── Home.vue
│   │   ├── Register.vue
│   │   ├── Scan.vue
│   │   └── Records.vue
│   ├── App.vue
│   └── main.ts
├── .env                     # 环境变量
├── .env.development
├── .env.production
├── index.html
├── package.json
├── tsconfig.json
├── vite.config.ts
└── README.md
```

## 四、实现步骤

### 4.1 创建项目

```bash
# 使用 Vite 创建项目
npm create vite@latest frontend -- --template vue-ts

cd frontend
```

### 4.2 安装依赖

```bash
# 安装核心依赖
npm install vue-router@4 pinia axios

# 安装 UI 组件库
npm install element-plus
npm install @element-plus/icons-vue

# 安装类型定义
npm install -D @types/node
```

### 4.3 package.json

```json
{
  "name": "face-scan-frontend",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "vue": "^3.4.0",
    "vue-router": "^4.2.0",
    "pinia": "^2.1.0",
    "axios": "^1.6.0",
    "element-plus": "^2.4.0",
    "@element-plus/icons-vue": "^2.3.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.0",
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0",
    "vite": "^5.0.0",
    "vue-tsc": "^1.8.0"
  }
}
```

### 4.4 Vite 配置 (vite.config.ts)

```typescript
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'
import { resolve } from 'path'

export default defineConfig({
  plugins: [vue()],
  
  resolve: {
    alias: {
      '@': resolve(__dirname, 'src')
    }
  },
  
  server: {
    port: 5173,
    host: '0.0.0.0',
    proxy: {
      '/api': {
        target: 'http://localhost:8000',
        changeOrigin: true
      },
      '/ws': {
        target: 'ws://localhost:8000',
        ws: true
      }
    }
  },
  
  build: {
    outDir: 'dist',
    sourcemap: false,
    chunkSizeWarningLimit: 1500,
    rollupOptions: {
      output: {
        manualChunks: {
          'element-plus': ['element-plus'],
          'vue-vendor': ['vue', 'vue-router', 'pinia']
        }
      }
    }
  }
})
```

### 4.5 TypeScript 配置 (tsconfig.json)

```json
{
  "compilerOptions": {
    "target": "ES2020",
    "useDefineForClassFields": true,
    "module": "ESNext",
    "lib": ["ES2020", "DOM", "DOM.Iterable"],
    "skipLibCheck": true,
    
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "noEmit": true,
    "jsx": "preserve",
    
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true,
    
    "baseUrl": ".",
    "paths": {
      "@/*": ["src/*"]
    }
  },
  "include": ["src/**/*.ts", "src/**/*.tsx", "src/**/*.vue"],
  "references": [{ "path": "./tsconfig.node.json" }]
}
```

### 4.6 环境变量

**.env.development**
```env
VITE_API_BASE_URL=http://localhost:8000
VITE_WS_URL=ws://localhost:8000/ws/face-stream
```

**.env.production**
```env
VITE_API_BASE_URL=https://your-domain.com
VITE_WS_URL=wss://your-domain.com/ws/face-stream
```

### 4.7 主入口 (src/main.ts)

```typescript
import { createApp } from 'vue'
import { createPinia } from 'pinia'
import ElementPlus from 'element-plus'
import 'element-plus/dist/index.css'
import * as ElementPlusIconsVue from '@element-plus/icons-vue'

import App from './App.vue'
import router from './router'
import './assets/styles/main.css'

const app = createApp(App)

// 注册 Pinia
app.use(createPinia())

// 注册路由
app.use(router)

// 注册 Element Plus
app.use(ElementPlus)

// 注册图标
for (const [key, component] of Object.entries(ElementPlusIconsVue)) {
  app.component(key, component)
}

app.mount('#app')
```

### 4.8 根组件 (src/App.vue)

```vue
<template>
  <div id="app">
    <el-config-provider :locale="zhCn">
      <router-view />
    </el-config-provider>
  </div>
</template>

<script setup lang="ts">
import { ElConfigProvider } from 'element-plus'
import zhCn from 'element-plus/dist/locale/zh-cn.mjs'
</script>

<style>
#app {
  width: 100%;
  height: 100vh;
}
</style>
```

### 4.9 全局样式 (src/assets/styles/main.css)

```css
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

html, body {
  width: 100%;
  height: 100%;
  font-family: 'Helvetica Neue', Helvetica, 'PingFang SC', 'Hiragino Sans GB', 'Microsoft YaHei', Arial, sans-serif;
}

/* 滚动条样式 */
::-webkit-scrollbar {
  width: 8px;
  height: 8px;
}

::-webkit-scrollbar-thumb {
  background-color: #dcdfe6;
  border-radius: 4px;
}

::-webkit-scrollbar-thumb:hover {
  background-color: #c0c4cc;
}
```

## 五、目录创建

```bash
# 创建目录结构
cd frontend

mkdir -p src/{api,assets/styles,components,composables,router,stores,types,utils,views}
mkdir -p public

# 创建文件
touch src/{main,App}.ts
touch src/router/index.ts
touch src/assets/styles/main.css
touch .env.development .env.production
```

## 六、验收标准

- [ ] `npm run dev` 可正常启动开发服务器
- [ ] 访问 http://localhost:5173 可看到 Vue 页面
- [ ] TypeScript 编译无错误
- [ ] Element Plus 组件正常显示
- [ ] API 代理配置生效
- [ ] 环境变量可正常读取

## 七、常见问题

### 7.1 依赖安装慢
```bash
# 使用国内镜像
npm config set registry https://registry.npmmirror.com
```

### 7.2 TypeScript 类型错误
```bash
# 安装 Vue 类型
npm install -D @vue/runtime-core
```

### 7.3 Vite 端口冲突
```bash
# 修改 vite.config.ts 中的 server.port
```

## 八、下一步

完成项目初始化后，继续实现：
- **01-路由配置**: 配置页面路由
- **02-状态管理**: 设置 Pinia store
- **03-API封装**: 封装 Axios 请求
